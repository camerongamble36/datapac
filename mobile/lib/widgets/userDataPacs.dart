import 'package:flutter/material.dart';

class UserDataPacs extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('User Data Pacs List'),
      ),
    );
  }
}
